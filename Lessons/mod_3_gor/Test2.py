def generate_page(head, body):
    page = f"<html>{head}{body}</html>"
    return page
print('\n')

def generate_page(head, body):
    page = f"<html>+head+body+</html>"
    return page
print('\n')

def generate_page(head, body):
    page = f"""<html>
    {head}
    {body}
    </html>"""
    return page
print('\n')

generate_page(head, body)